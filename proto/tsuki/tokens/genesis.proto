syntax = "proto3";
package tsuki.tokens;

import "alias.proto";
import "rate.proto";

option go_package = "github.com/TsukiCore/tsuki/x/tokens/types";

message GenesisState {
  map<string, tsuki.tokens.TokenAlias> aliases = 1;
  map<string, tsuki.tokens.TokenRate> rates = 2;
}
